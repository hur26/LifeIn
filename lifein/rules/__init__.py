"""主动触发的规则层。

架构 §4 说触发方式有五种,这里管的是其中一种:**规则扫描** ——
在已有数据上定期匹配条件,命中就推一条。

**规则只回答"该不该说这句话",不决定"要不要真的推出去"。** 后面那件事由
调度层统一处理:影子模式、频率闸门、去重都在 `jobs/reminders.py` 里。
分开是因为那三条是**全局约束**,写进每条规则就等于每加一条规则都要重新遵守
一遍 —— 而漏掉一次的代价是用户被吵到,然后关掉全部通知(R4)。

加一条新规则只该改两处:写一个模块,在 `builtin.py` 的清单里加一行。
它默认处在 `shadow`(没有 `rule_state` 记录就是 shadow),所以**新规则
上线的第一周不会打扰任何人**,这不是靠自觉,是靠默认值。
"""

from lifein.rules.base import Reminder, Rule, RuleContext

__all__ = ["Reminder", "Rule", "RuleContext"]
