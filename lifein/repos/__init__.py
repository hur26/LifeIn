"""数据访问层。

**铁律 1 在这里强制**:每个公开函数的第一个参数都是 `user_id`。
不是风格问题 —— 租户隔离靠这一层保证,而"忘了带 user_id"是那种一旦发生
就没有任何报错、只会静默串数据的错误。`tests/test_repo_contract.py`
用签名检查盯着它,新加的函数漏了会红。

分工按 ADR-016:简单 CRUD 用 SQLAlchemy,记忆层那些 JSONB 与向量的分析
查询直接写 SQL。
"""
